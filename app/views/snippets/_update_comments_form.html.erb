<%= form_for :comment, url: "/comments/#{comment.id}", method: :put do |f| %>
    <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snippet.errors.count, "error") %> prohibited this snippet from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div class="form-group update-comment-formgroup">
        <%= f.text_area :comment_body, class: "form-control", value: comment.comment_body  %>
    </div>
    <div class="form-group update-comment-formgroup">
      <input type="submit" class="btn btn-primary btn-sm" value="Update Comment">
    </div>
  </div>
<% end %>